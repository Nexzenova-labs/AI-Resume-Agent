# Project Config Mirror

This mirrors the current root-level `project_config.md` so the repository layout also matches the path referenced in the README.

Environment variables and flags are currently defined in:

- `/project_config.md`
- `/.env.example`
- `/backend/.env.example`
- `/frontend/.env.local.example`

